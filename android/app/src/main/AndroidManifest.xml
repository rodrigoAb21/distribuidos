<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.distribuidos.uagrm.android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.AsignacionActivity"
            android:label="Asignaciones" />
        <activity
            android:name=".activities.EncuestaActivity"
            android:label="Encuestas"
            android:parentActivityName=".activities.AsignacionActivity" />
        <activity
            android:name=".activities.FormularioActivity"
            android:label="Nueva encuesta" />
        <activity android:name=".activities.AreaActivity"></activity>
    </application>

</manifest>